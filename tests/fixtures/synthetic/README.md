# Synthetic Audit Fixtures

These files were created for this project and contain no microscopy pixels.

- `malformed/not_a_lif.lif` is intentionally invalid and is used to confirm
  that metadata inspection fails without creating conversion output.

The valid LIF fixtures are public samples documented in
`tests/fixtures/PUBLIC_FIXTURES.md`.
